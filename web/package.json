{
  "name": "disc-golf-flight-lab-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Landing page and static host for the Disc Golf Flight Lab Godot web export.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "export:game": "mkdir -p public/game && cd ../game && \"${GODOT:-godot}\" --headless --export-release Web ../web/public/game/index.html",
    "build:all": "npm run export:game && npm run build"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
